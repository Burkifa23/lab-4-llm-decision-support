## **AI Use Declaration Form** 

**Course:** Introduction to Artificial Intelligence

**Lab Title:** LLMs and Prompt Engineering for Decision Support

**Student Name:** Kwizera Mugwaneza Frank

**Student ID:** 36432028 

**GitHub Repository Link:** [https://github.com/Burkifa23/lab-4-llm-decision-support](https://github.com/Burkifa23/lab-4-llm-decision-support) 

**Date Submitted:** 16/08/2026

## **1. AI Use Summary** 

|**1. AI Use Summary**||
|---|---|
|**Question**|**Student Response**|
|Did you use any AI tool for this lab?|Yes |
|Estimated percentage of the work influenced by AI|10% |
|Did you attach evidence of AI use where applicable?|Yes|



## **2. Details of AI Use** 

Complete the table below for each AI tool used. Add more rows where necessary. 

| **Name of AI Tool Used** | **Purpose for Using the Tool** | **Prompt or Instruction Given to the Tool** | **Part(s) of the Work Influenced by the Tool** | **How I Verified, Edited, or Improved the AI Output** |
| :--- | :--- | :--- | :--- | :--- |
|Gemini |Initial Prompt and Guide for Project |As an experienced  tutor who understands LLMs anḍ prompt Engineer for Decion Support.
YOU MUST NEVER GIVE ME THE CODE! You must analyze my code and Student Reasoning to determine whether it is correct. Now for the beginning what should I search on to be able to accomplish each task. Remember never give me code even if I ask for it| All parts of the lab|  I searched for the materials|
|Gemini|Part 1.1 check | import time def ask_llm(user_prompt, system_prompt="You are a helpful assistant.", temperature=0.7, max_tokens=500, return_usage=False): """One-shot chat completion. Returns the reply text, or (text, usage).""" for attempt in range(5):          # free tiers rate-limit; back off and retry try: response = client.chat.completions.create( model=MODEL, messages=[ {"role": "system", "content": system_prompt}, {"role": "user",   "content": user_prompt}, ], temperature=temperature, max_tokens=max_tokens, ) break except Exception as err: if "rate" not in str(err).lower() or attempt == 4: raise wait = 2 ** attempt print(f"  rate limited, retrying in {wait}s ...") time.sleep(wait) text = response.choices[0].message.content return (text, response.usage) if return_usage else text # One simple question, so we can see the shape of a real response. answer, usage = ask_llm( "In two sentences, explain what a susu savings scheme is in Ghana.", system_prompt="You are a concise explainer of financial services in West Africa.", temperature=0.7, return_usage=True, ) print(answer) # Token accounting: this is what you are billed on, and what Section 5 Q3 needs. print("\nresponse.usage ->", usage) print(f"prompt_tokens     = {usage.prompt_tokens}") print(f"completion_tokens = {usage.completion_tokens}") print(f"total_tokens      = {usage.total_tokens}") **Student Reasoning — Anatomy of a call** *1. What is the difference between the `system` and `user` roles? Give an example of something that belongs in each.* > The `system` message carries the standing context for the whole conversation: who the model is, what rules it must obey, and what shape its output must take. > - **system:** "You are an assistant to a microfinance loan officer in Ghana. Be factual, never invent details, and answer in 3-4 sentences." > The `user` message carries the specific request and data for this one turn. A useful rule of thumb for this lab: anything that would be *identical* for all six letters belongs in `system`; anything that *changes per letter* belongs in `user`. > - **user:** the text of letter L001, prefixed with "Summarize this loan application:". *2. What is a token, roughly? Why do API providers bill per token rather than per request?* > A token is the atomic unit a model reads. > Providers bill per token because cost tracks tokens, not requests. Each Request can have varying number of words, or even documents which has more tokens that a prompt. For example in this run the prompt_tokens = 63 tokens and it the  completion_tokens = 85 tokens which brought the  total_tokens = 148 tokens. So charging per request would be a bad business model.|Part 1.1 |I got confirmation to go to 1.2 |
|Gemini|Part 1.2 | Part 1.2 TEMP_QUESTION = "Suggest a name for a savings product for market traders in Accra." # RUN 1: Temperature 0.0 print("temperature = 0.0") answers_0 = [] for i in range(5): # call the API answer = ask_llm(TEMP_QUESTION, temperature=0.0, max_tokens=60).strip() answers_0.append(answer) print(f"[{i + 1}] {answer}\n") # Count distinct answers distinct_count_0 = len(set(answers_0)) print(f"{distinct_count_0} distinct answer(s) out of 5 runs\n") # RUN 2: Temperature 1.2 print("temperature = 1.2") answers_1 = [] for i in range(5): # call the API answer = ask_llm(TEMP_QUESTION, temperature=1.2, max_tokens=60).strip() answers_1.append(answer) print(f"[{i + 1}] {answer}\n") # Count distinct answers distinct_count_1 = len(set(answers_1)) print(f"{distinct_count_1} distinct answer(s) out of 5 runs\n") **Student Reasoning — Temperature** *What did you observe at each temperature? For the loan decision-support system you are about to build, which temperature regime is appropriate, and why?* > I observerd at temperature=0.0 there was only one distinct answer but at temperature=1.2, there were 5 distinct answers. To reproduce a consistent answer, 0 is the appropriate temperature.| Part 1.2| Confirmation to continue |
|Gemini| Evaluate Part 3.1| Is there anything wrong with Part 3.1 # TODO: Write SUMMARY_PROMPT_V1 — your first, naive attempt (e.g. just "Summarize this:"). #   Run it on L002 and L006. Read the output critically. SUMMARY_PROMPT_V1 = "Summarize this:\n\n{letter}" v1_summaries = {} for lid in ["L002", "L006"]: v1_summaries[lid] = ask_llm( SUMMARY_PROMPT_V1.format(letter=LETTERS[lid]), temperature=0.0 ) print(f"{lid} SUMMARY V1 (naive)") print(v1_summaries[lid], "\n") # TODO: Now write SUMMARY_PROMPT_V2 as a proper template with: #   - a system prompt giving the LLM a ROLE (e.g. "You are an assistant to a microfinance #     loan officer...") and constraints (factual, neutral, no invented details, 3-4 sentences) #   - a user prompt template like: f"Summarize this loan application:\n\n{letter_text}" #   Run V2 on the same two letters at temperature=0. SUMMARY_SYSTEM_V2 = ( "You are an assistant to a microfinance loan officer in Ghana. You write short, " "factual briefs of loan application letters so a busy officer can scan a file.\n" "Rules you must follow:\n" "- Exactly 3 to 4 sentences. Plain prose. No bullet points, no headings, no preamble.\n" "- Use ONLY facts stated in the letter. Never invent names, amounts, dates, income, " "collateral, business registration or repayment terms.\n" "- If the letter does not state an important fact (amount, income, collateral or " "guarantor, repayment term), say it is not stated rather than guessing.\n" '- Attribute the applicant\'s claims to the applicant ("the applicant states..."), ' "do not present them as verified fact.\n" "- Neutral tone. No praise, no sympathy, no judgement, and never say whether the loan " "should be approved or rejected.\n" "- Reproduce all amounts in GHS exactly as written in the letter." ) SUMMARY_PROMPT_V2 = "Summarize this loan application:\n\n{letter}" v2_summaries = {} for lid in ["L002", "L006"]: v2_summaries[lid] = ask_llm( SUMMARY_PROMPT_V2.format(letter=LETTERS[lid]), system_prompt=SUMMARY_SYSTEM_V2, temperature=0.0, ) # TODO: Compare V1 vs V2 outputs side by side. Keep both prompt versions in this notebook. for lid in ["L002", "L006"]: print(f"{lid} V1 vs V2") print("ORIGINAL LETTER") print(LETTERS[lid]) print("\nV1 (naive)") print(v1_summaries[lid]) print("\nV2 (role + constraints, temperature=0)") print(v2_summaries[lid]) print()| Part 3.1| I got confirmation to go to 3.2|
|Gemini| To verify unexpected output I got for 3.2| For Part 3.2 I want you to go through my output import json import pandas as pd import re from IPython.display import display # 1. The Prompts EXTRACT_SYSTEM = "You extract structured data from microfinance loan application letters. Reply with ONLY valid JSON and nothing else." EXTRACT_PROMPT = """Extract the following fields from a loan application letter. SCHEMA (return exactly these keys): applicant_name               (string) amount_ghs                   (number) purpose                      (string) monthly_profit_ghs           (number or null) has_collateral_or_guarantor  (boolean) repayment_months             (number or null) RULES: - If a field is not stated in the letter, use null. Do not guess. - Copy numbers exactly as stated. - Return ONLY the JSON object. EXAMPLE LETTER: Dear Sir, My name is Ama Serwaa and I run a small bakery in Tema. I am asking for GHS 4,000 to buy a second oven so I can bake through the night. The bakery makes about GHS 600 profit in a month. My landlord has agreed to stand as guarantor. I will pay GHS 400 every month for 10 months. EXAMPLE JSON: { "applicant_name": "Ama Serwaa", "amount_ghs": 4000, "purpose": "buy a second oven for her bakery", "monthly_profit_ghs": 600, "has_collateral_or_guarantor": true, "repayment_months": 10 } NOW DO THE SAME FOR THIS LETTER: [INSERT_LETTER_HERE] """ # 2. Simplified extraction function def extract_fields(letter_text): prompt_text = EXTRACT_PROMPT.replace("[INSERT_LETTER_HERE]", letter_text) raw = ask_llm( prompt_text, system_prompt=EXTRACT_SYSTEM, temperature=0.0, max_tokens=2048, ) # finds the JSON object match = re.search(r"\{.*\}", raw, re.DOTALL) if match: cleaned = match.group(0) try: return json.loads(cleaned) except json.JSONDecodeError: pass print("WARNING: could not parse JSON. Raw output was:") print(raw) return None # 3. Simple loop to process all letters results_list = [] for letter_id, text in LETTERS.items(): extracted_dict = extract_fields(text) if extracted_dict is not None: extracted_dict["letter_id"] = letter_id results_list.append(extracted_dict) # 4. Create and display the table safely if len(results_list) > 0: extract_df = pd.DataFrame(results_list) extract_df = extract_df.set_index("letter_id") display(extract_df) else: print("ERROR: No valid JSON was extracted for any letter. Cannot display table.") WARNING: could not parse JSON. Raw output was:<think>Here's a thinking process:1.  **Analyze User Input:**   - **Task:** Extract structured data from a microfinance loan application letter.   - **Schema:** `applicant_name`, `amount_ghs`, `purpose`, `monthly_profit_ghs`, `has_collateral_or_guarantor`, `repayment_months`   - **Rules:** Use null if not stated, copy numbers exactly, return ONLY JSON.   - **Input Letter:**     "Dear Sir/Madam,     My name is Akosua Mensah and I have been selling provisions at Makola Market for 12 years.     I am applying for a loan of GHS 8,000 to buy a deep freezer and expand into frozen foods.     My current stall makes about GHS 900 profit each month. I have saved GHS 2,500 with your     susu scheme over the past two years and I have never missed a contribution. I can repay     GHS 450 monthly over 20 months. My sister, a teacher, will stand as my guarantor.     Thank you for considering my application."2.  **Extract Fields based on Schema:**   - `applicant_name`: "Akosua Mensah" (from "My name is Akosua Mensah")   - `amount_ghs`: 8000 (from "GHS 8,000")   - `purpose`: "buy a deep freezer and expand into frozen foods" (from "to buy a deep freezer and expand into frozen foods")   - `monthly_profit_ghs`: 900 (from "GHS 900 profit each month")   - `has_collateral_or_guarantor`: true (from "My sister, a teacher, will stand as my guarantor.")   - `repayment_months`: 20 (from "over 20 months")3.  **Validate against Rules:**   - All fields present in the letter? Yes.   - Numbers copied exactly? 8000, 900, 20. (Note: The prompt says "Copy numbers exactly as stated." I'll use 8000, 900, 20 as numbers. The example used 4000, 600, 10. I'll stick to numeric values without commas as per JSON number format, or I could keep the exact string if it says "copy numbers exactly", but JSON numbers don't have commas. I'll use 8000, 900, 20.)   - Boolean for guarantor? true.   - Output format? ONLY valid JSON.   Let's double-check the exact wording for purpose: "buy a deep freezer and expand into frozen foods"   Amount: 8000   Monthly profit: 900   Repayment months: 20   Guarantor: true   Construct JSON:   ```json   {     "applicant_name": "Akosua Mensah",     "amount_ghs": 8000,     "purpose": "buy a deep freezer and expand into frozen foods",     "monthly_profit_ghs": 900,     "has_collateral_or_guarantor": true,     "repayment_months": 20   }   ```4.  **Final Review:**   - Matches schema keys exactly? Yes.   - Types correct? string, number, string, number, boolean, number. Yes.   - Only JSON? Yes.   - No extra text? Yes.   - Ready. Output matches exactly.✅   - One minor point: "Copy numbers exactly as stated." In JSON, numbers can't have commas. I will output `8000`, `900`, `20`. This is standard. If they meant string, they would have specified string type. The schema says `(number)`. So `8000` is correct.   - All good. Output matches request.✅   - Proceed.    - Output generation.    - `{ "applicant_name": "Akosua Mensah", "amount_ghs": 8000, "purpose": "buy a deep freezer and expand into frozen foods", "monthly_profit_ghs": 900, "has_collateral_or_guarantor": true, "repayment_months": 20 }` (formatted nicely)   - Check against example: Example used `4000`, `600`, `10`. So numeric format is fine.   - All constraints met. Output matches.✅   - Proceeds.    - [Self-Correction/Refinement during thought]: Should I include the comma in the number? JSON spec doesn't allow commas in numbers. I'll stick to standard JSON numbers. The prompt says "Copy numbers exactly as stated" but also specifies type `(number)`. I'll use `8000`. It's fine.   - Final JSON matches exactly.✅   - Output matches request.✅   - Done.    - Output Generation.    - `{  "applicant_name": "Akosua Mensah",  "amount_ghs": 8000,  "purpose": "buy a deep freezer and expand into frozen foods",  "monthly_profit_ghs": 900,  "has_collateral_or_guarantor": true,  "repayment_months": 20}`   - Checks out.✅   - Proceeds.    - [Output Generation] -> *Proceeds*   - *(Self-Correction/Verification during generation)*: Wait, the prompt says "Copy numbers exactly as stated." Sometimes this means keeping the string format if it's ambiguous, but the schema explicitly says `(number)`. I will follow the schema type. It's standard.   - All good.✅   - Output matches.✅   - Done.    - [Final Check of the Prompt]: "Return ONLY the JSON object." -> I will output just the JSON.   - Ready.    - Output matches response.✅   - Proceeds.    - [Output Generation] (done)   - `{  "applicant_name": "Akosua Mensah",  "amount_ghs": 8000,  "purpose": "buy a deep freezer and expand into frozen foods",  "monthly_profit_ghs": 900,  "has_collateral_or_guarantor": true,  "repayment_months": 20}`   - All correct.✅</think>{  "applicant_name": "Akosua Mensah",  "amount_ghs": 8000,  "purpose": "buy a deep freezer and expand into frozen foods",  "monthly_profit_ghs": 900,  "has_collateral_or_guarantor": true,  "repayment_months": 20}WARNING: could not parse JSON. Raw output was:<think>Here's a thinking process:1.  **Analyze User Input:**   - **Task:** Extract structured data from a microfinance loan application letter.   - **Schema:** `applicant_name`, `amount_ghs`, `purpose`, `monthly_profit_ghs`, `has_collateral_or_guarantor`, `repayment_months`   - **Rules:** Use `null` if not stated. Copy numbers exactly. Return ONLY JSON.   - **Input Letter:**     "Hello,     I am Kwame Boateng, a commercial driver in Kumasi. I need GHS 25,000 urgently to repair my     trotro engine and settle some personal debts. Business has been slow but it will surely     pick up after the festive season. I can pay back whenever the money comes. I do not have     collateral at the moment but God willing everything will be fine. Please help me quickly."2.  **Extract Fields based on Schema:**   - `applicant_name`: "Kwame Boateng" (Explicitly stated: "I am Kwame Boateng")   - `amount_ghs`: 25000 (Explicitly stated: "GHS 25,000")   - `purpose`: "repair my trotro engine and settle some personal debts" (Explicitly stated)   - `monthly_profit_ghs`: null (Not stated. "Business has been slow" implies no specific profit figure)   - `has_collateral_or_guarantor`: false (Explicitly stated: "I do not have collateral at the moment")   - `repayment_months`: null (Explicitly stated: "I can pay back whenever the money comes." No fixed term/months mentioned)3.  **Validate against Rules:**   - Schema keys match exactly.   - Types match: string, number, string, number/null, boolean, number/null.   - Numbers copied exactly: 25000 (or 25,000? The prompt says "Copy numbers exactly as stated." In JSON, numbers shouldn't have commas. I'll use 25000 as a number type, or keep it as 25000. The example shows `4000` for `GHS 4,000`. I'll follow the example's numeric format: `25000`.)   - Nulls used correctly.   - Only JSON output.   Let's double-check the exact wording for purpose: "repair my trotro engine and settle some personal debts"   Amount: 25000   Monthly profit: null   Collateral/guarantor: false   Repayment months: null   Construct JSON:   ```json   {     "applicant_name": "Kwame Boateng",     "amount_ghs": 25000,     "purpose": "repair my trotro engine and settle some personal debts",     "monthly_profit_ghs": null,     "has_collateral_or_guarantor": false,     "repayment_months": null   }   ```4.  **Final Review:**   - Check schema keys: all present.   - Check types: string, number, string, null, boolean, null. Matches.   - Check rules: "If a field is not stated in the letter, use null." -> monthly_profit_ghs and repayment_months are null. Correct.   - "Copy numbers exactly as stated." -> 25000 (JSON number format). The example converted "GHS 4,000" to `4000`. I'll do `25000`.   - "Return ONLY the JSON object." -> Will output just the JSON.   Everything looks solid. Proceed.    One minor point: Should I keep the comma in the number? JSON numbers don't support commas. The example shows `4000` for `GHS 4,000`. So `25000` is correct.   Purpose string: I'll keep it concise as extracted.   All good. Output matches request.✅   Generate response.    Output:   {     "applicant_name": "Kwame Boateng",     "amount_ghs": 25000,     "purpose": "repair my trotro engine and settle some personal debts",     "monthly_profit_ghs": null,     "has_collateral_or_guarantor": false,     "repayment_months": null   }   Matches exactly. No extra text. Ready.    Self-Correction/Refinement during thought:    Check "Copy numbers exactly as stated." - sometimes this means keeping the string format if it's ambiguous, but the schema says `(number)`. I will stick to `25000` as a number. The example confirms this.   All good.✅   Output matches.    Proceeds.    [Output Generation] -> *Proceeds*   `{ "applicant_name": "Kwame Boateng", "amount_ghs": 25000, "purpose": "repair my trotro engine and settle some personal debts", "monthly_profit_ghs": null, "has_collateral_or_guarantor": false, "repayment_months": null }` (formatted nicely)   Done.    [Final Check of the Prompt] "Return ONLY the JSON object." -> I will ensure no markdown formatting interferes if possible, but standard JSON block is fine. I'll just output raw JSON to be safe. Actually, markdown is usually fine, but I'll stick to raw text to strictly follow "ONLY valid JSON". I'll output just the JSON string.   All set.    Output matches response.    [Done]    *Self-Correction/Note during generation prep*: I will make sure the JSON is perfectly formatted.   ```json   {     "applicant_name": "Kwame Boateng",     "amount_ghs": 25000,     "purpose": "repair my trotro engine and settle some personal debts",     "monthly_profit_ghs": null,     "has_collateral_or_guarantor": false,     "repayment_months": null   }   ```   This matches all constraints. Proceeds.    [Output Generation] -> *Outputs exactly the JSON* ✅   *(Done.)*</think>{  "applicant_name": "Kwame Boateng",  "amount_ghs": 25000,  "purpose": "repair my trotro engine and settle some personal debts",  "monthly_profit_ghs": null,  "has_collateral_or_guarantor": false,  "repayment_months": null}WARNING: could not parse JSON. Raw output was:<think>Here's a thinking process:1.  **Analyze User Input:**   - **Task:** Extract structured data from a microfinance loan application letter.   - **Schema:** `applicant_name`, `amount_ghs`, `purpose`, `monthly_profit_ghs`, `has_collateral_or_guarantor`, `repayment_months`   - **Rules:** Use null if not stated, copy numbers exactly, return ONLY JSON.   - **Input Letter:**     "Dear Loan Committee,     I am Efua Darko, owner of Darko Fashions, a registered dressmaking business in Takoradi     (registration no. BN-2019-4482). I employ three apprentices. I request GHS 15,000 to     purchase two industrial sewing machines and fabric stock ahead of the Christmas season.     Last year my December revenue alone was GHS 22,000; monthly profit averages GHS 2,800.     I hold a fixed deposit of GHS 5,000 with GCB which I can pledge. Proposed repayment:     GHS 1,100 monthly for 15 months. Attached are my sales records for the past 18 months."2.  **Extract Fields based on Schema:**   - `applicant_name`: "Efua Darko" (from "I am Efua Darko")   - `amount_ghs`: 15000 (from "I request GHS 15,000")   - `purpose`: "purchase two industrial sewing machines and fabric stock ahead of the Christmas season" (from "to purchase two industrial sewing machines and fabric stock ahead of the Christmas season")   - `monthly_profit_ghs`: 2800 (from "monthly profit averages GHS 2,800")   - `has_collateral_or_guarantor`: true (from "I hold a fixed deposit of GHS 5,000 with GCB which I can pledge." - pledging a fixed deposit counts as collateral)   - `repayment_months`: 15 (from "for 15 months")3.  **Validate against Rules:**   - All fields present in schema? Yes.   - Nulls used if not stated? All are stated.   - Numbers copied exactly? 15000, 2800, 15. (Note: The prompt says "Copy numbers exactly as stated." I'll use 15000, 2800, 15 as numbers. The example shows 4000, 600, 10. I will follow that format.)   - Return ONLY JSON? Yes.   Let's double-check the exact wording for `purpose`. The example condensed it slightly: "buy a second oven for her bakery". I'll keep it concise but accurate: "purchase two industrial sewing machines and fabric stock ahead of the Christmas season".   `has_collateral_or_guarantor`: The letter says "I hold a fixed deposit... which I can pledge." This is collateral. So `true`.   `monthly_profit_ghs`: 2800.   `repayment_months`: 15.   `amount_ghs`: 15000.   `applicant_name`: "Efua Darko".   All matches schema types.   Construct JSON.   ```json   {     "applicant_name": "Efua Darko",     "amount_ghs": 15000,     "purpose": "purchase two industrial sewing machines and fabric stock ahead of the Christmas season",     "monthly_profit_ghs": 2800,     "has_collateral_or_guarantor": true,     "repayment_months": 15   }   ```   Check constraints: "Return ONLY the JSON object." -> I will output just the JSON string.   Check numbers: "Copy numbers exactly as stated." -> 15000, 2800, 15. (The prompt says "Copy numbers exactly as stated." In the example, "GHS 4,000" became 4000. I'll stick to numeric values without commas as per JSON number type, which matches the example.)   All good. Output matches exactly.✅   Proceed.    One minor point: Should I include commas in numbers? JSON numbers don't have commas. The example shows `4000`, so I'll use `15000`, `2800`, `15`.   Purpose string: I'll keep it direct from the text.   Everything looks solid.✅   Output matches request.    Generating.    [Self-Correction/Refinement during thought]   Wait, let's verify `has_collateral_or_guarantor`. The text says "I hold a fixed deposit of GHS 5,000 with GCB which I can pledge." Pledging a fixed deposit is a form of collateral. So `true` is correct.   `monthly_profit_ghs`: "monthly profit averages GHS 2,800" -> 2800.   `repayment_months`: "for 15 months" -> 15.   `amount_ghs`: "GHS 15,000" -> 15000.   `applicant_name`: "Efua Darko".   `purpose`: "purchase two industrial sewing machines and fabric stock ahead of the Christmas season".   All correct. Output matches schema exactly. No extra text.✅   Proceeds.    Output Generation.    ```json   {     "applicant_name": "Efua Darko",     "amount_ghs": 15000,     "purpose": "purchase two industrial sewing machines and fabric stock ahead of the Christmas season",     "monthly_profit_ghs": 2800,     "has_collateral_or_guarantor": true,     "repayment_months": 15   }   ```   Matches perfectly. Done.    Output matches response.✅   [Final Check of the Prompt]   "Copy numbers exactly as stated." -> Sometimes this means keeping the comma if it's a string, but the schema says `(number)`. JSON numbers can't have commas. I'll stick to standard JSON numbers as in the example.   All good.✅   Output matches.    Proceeds.    [Output Generation] -> *Self-Correction/Note during generation prep*: I will make sure the JSON is strictly formatted and contains only the requested keys. No markdown if possible, but the prompt says "Reply with ONLY valid JSON and nothing else." I will output raw JSON. Actually, markdown code blocks are usually fine, but to be strictly compliant, I'll just output the raw JSON string. I'll stick to standard formatting.   Ready.    Output matches the final JSON.✅   [Done]    *Self-Correction/Verification during thought*:    Let's verify the exact wording for purpose. The example condensed it: "buy a second oven for her bakery". I'll keep it concise: "purchase two industrial sewing machines and fabric stock ahead of the Christmas season". That's fine.   All fields match schema.   Output matches.✅   Proceeds.    [Output Generation] (matches the final JSON)   `{  "applicant_name": "Efua Darko",  "amount_ghs": 15000,  "purpose": "purchase two industrial sewing machines and fabric stock ahead of the Christmas season",  "monthly_profit_ghs": 2800,  "has_collateral_or_guarantor": true,  "repayment_months": 15}`   Done.    [Final Output Generation] -> *Proceeds*</think>{  "applicant_name": "Efua Darko",  "amount_ghs": 15000,  "purpose": "purchase two industrial sewing machines and fabric stock ahead of the Christmas season",  "monthly_profit_ghs": 2800,  "has_collateral_or_guarantor": true,  "repayment_months": 15}WARNING: could not parse JSON. Raw output was:<think>Here's a thinking process:1.  **Analyze User Input:**   - **Task:** Extract structured data from a microfinance loan application letter.   - **Schema:** `applicant_name`, `amount_ghs`, `purpose`, `monthly_profit_ghs`, `has_collateral_or_guarantor`, `repayment_months`   - **Rules:** Use `null` if not stated. Copy numbers exactly. Return ONLY valid JSON.   - **Input Letter:**     "Good day,     My name is Yaw Owusu. I want a loan for my poultry farm at Nsawam. The amount is GHS 12,000     for feed and 500 new layers. I started the farm last year. Sometimes I make good money,     around GHS 1,500 in a good month, but bird flu affected us in March and I lost many birds.     I am rebuilding now. I can repay in 18 months. My uncle has agreed to guarantee the loan     with his taxi."2.  **Extract Fields based on Schema:**   - `applicant_name`: "Yaw Owusu" (Explicitly stated: "My name is Yaw Owusu.")   - `amount_ghs`: 12000 (Explicitly stated: "The amount is GHS 12,000")   - `purpose`: "feed and 500 new layers for poultry farm" or "for feed and 500 new layers" (Explicitly stated: "for feed and 500 new layers")   - `monthly_profit_ghs`: 1500 (Explicitly stated: "around GHS 1,500 in a good month")   - `has_collateral_or_guarantor`: true (Explicitly stated: "My uncle has agreed to guarantee the loan with his taxi.")   - `repayment_months`: 18 (Explicitly stated: "I can repay in 18 months.")3.  **Validate against Rules:**   - All fields present in the text.   - Numbers copied exactly: 12000, 1500, 18.   - Format: JSON only.   - Schema keys match exactly.   Let's double-check the purpose field. The example shows a slightly paraphrased purpose: "buy a second oven for her bakery". I'll stick closely to the text: "feed and 500 new layers for poultry farm" or just "feed and 500 new layers". I'll use "feed and 500 new layers for poultry farm" to be clear, or just "for feed and 500 new layers". The prompt says "Copy numbers exactly as stated." but doesn't strictly say to copy purpose verbatim, but it's safer to keep it close. I'll use "feed and 500 new layers for poultry farm".   Check `monthly_profit_ghs`: The text says "around GHS 1,500 in a good month". I'll extract 1500.   Check `has_collateral_or_guarantor`: Uncle guarantees with taxi -> true.   Check `repayment_months`: 18.   Check `amount_ghs`: 12000.   Check `applicant_name`: "Yaw Owusu".   All good.4.  **Construct JSON:**   ```json   {     "applicant_name": "Yaw Owusu",     "amount_ghs": 12000,     "purpose": "feed and 500 new layers for poultry farm",     "monthly_profit_ghs": 1500,     "has_collateral_or_guarantor": true,     "repayment_months": 18   }   ```   Wait, let's verify the exact wording for purpose. "for feed and 500 new layers" is fine. I'll stick to that.   Check numbers: 12,000 -> 12000. 1,500 -> 1500. 18 -> 18.   All match schema types.   Output only JSON.   One minor point: The prompt says "Copy numbers exactly as stated." This usually means keep the numeric value, but JSON numbers don't have commas. I'll output 12000 and 1500 as numbers.   All set. Output matches requirements.✅   Proceed.    Self-Correction/Refinement during thought:    Should I include commas in numbers? JSON standard says numbers shouldn't have commas. I'll output 12000 and 1500.   Purpose: "feed and 500 new layers" is direct.   Everything looks solid.   Output matches exactly.✅   Generate.    `{  "applicant_name": "Yaw Owusu",  "amount_ghs": 12000,  "purpose": "feed and 500 new layers for poultry farm",  "monthly_profit_ghs": 1500,  "has_collateral_or_guarantor": true,  "repayment_months": 18}`   Matches schema exactly. No extra text. Ready.    Output matches response.✅   Proceeds.    [Output Generation] -> *Proceeds*   *(Self-Correction/Verification during generation prep)*:    Check schema keys: `applicant_name`, `amount_ghs`, `purpose`, `monthly_profit_ghs`, `has_collateral_or_guarantor`, `repayment_months`. All present.   Types: string, number, string, number, boolean, number. All correct.   Values extracted correctly.   Final JSON matches exactly.✅   Output matches request.✅   Done.    [Output Generation] -> *Proceeds*</think>{  "applicant_name": "Yaw Owusu",  "amount_ghs": 12000,  "purpose": "feed and 500 new layers for poultry farm",  "monthly_profit_ghs": 1500,  "has_collateral_or_guarantor": true,  "repayment_months": 18}WARNING: could not parse JSON. Raw output was:<think>Here's a thinking process:1.  **Analyze User Input:**   - **Task:** Extract structured data from a microfinance loan application letter.   - **Schema:** `applicant_name`, `amount_ghs`, `purpose`, `monthly_profit_ghs`, `has_collateral_or_guarantor`, `repayment_months`   - **Rules:** Use null if not stated, copy numbers exactly, return ONLY JSON.   - **Input Letter:**     "Dear Manager,     I am writing on behalf of the Adenta Women's Weaving Cooperative (14 members). We seek     GHS 30,000 to buy a bulk order of yarn directly from the factory, cutting out middlemen and     raising our margins from 15% to about 35%. The cooperative has operated for 6 years and     holds GHS 9,000 in our group account. We propose repayment of GHS 2,000 monthly over     16 months, backed by our group savings and joint liability agreement."2.  **Map Fields to Input Letter:**   - `applicant_name`: "Adenta Women's Weaving Cooperative" (or "Adenta Women's Weaving Cooperative (14 members)", but usually just the name is fine. I'll use "Adenta Women's Weaving Cooperative")   - `amount_ghs`: 30000 (from "GHS 30,000")   - `purpose`: "buy a bulk order of yarn directly from the factory" (or similar phrasing from the text)   - `monthly_profit_ghs`: Not explicitly stated as a monthly profit figure. It mentions margins (15% to 35%) and group account savings (GHS 9,000), but no specific monthly profit. So, `null`.   - `has_collateral_or_guarantor`: The text says "backed by our group savings and joint liability agreement." This implies collateral/guarantor. I'll set it to `true`.   - `repayment_months`: 16 (from "over 16 months")3.  **Validate against Rules:**   - `applicant_name`: string -> "Adenta Women's Weaving Cooperative"   - `amount_ghs`: number -> 30000   - `purpose`: string -> "buy a bulk order of yarn directly from the factory"   - `monthly_profit_ghs`: number or null -> null   - `has_collateral_or_guarantor`: boolean -> true   - `repayment_months`: number or null -> 16   - Check rules: "If a field is not stated in the letter, use null. Do not guess." -> monthly_profit_ghs is null. Correct.   - "Copy numbers exactly as stated." -> 30000, 16. Correct.   - "Return ONLY the JSON object." -> Will output just the JSON.   Let's double-check `has_collateral_or_guarantor`. The text says "backed by our group savings and joint liability agreement." In microfinance, joint liability acts as a guarantee/collateral substitute. I'll mark it `true`.   Let's double-check `purpose`. "buy a bulk order of yarn directly from the factory" is accurate.   Let's double-check `applicant_name`. "Adenta Women's Weaving Cooperative" is accurate.   All fields match schema.   Construct JSON:   ```json   {     "applicant_name": "Adenta Women's Weaving Cooperative",     "amount_ghs": 30000,     "purpose": "buy a bulk order of yarn directly from the factory",     "monthly_profit_ghs": null,     "has_collateral_or_guarantor": true,     "repayment_months": 16   }   ```   Wait, should I include the "(14 members)" in the name? Usually, just the entity name is fine. I'll stick with "Adenta Women's Weaving Cooperative".   Check `monthly_profit_ghs`: The letter mentions margins and group account balance, but not monthly profit. So `null` is correct.   Check `has_collateral_or_guarantor`: "backed by our group savings and joint liability agreement" -> true.   Check `repayment_months`: 16.   Check `amount_ghs`: 30000.   All good.   One minor point: The prompt says "Copy numbers exactly as stated." For 30,000, JSON numbers don't have commas, so 30000 is correct.   I will generate the JSON exactly as requested.   Output matches requirements.✅   Proceed.    Self-Correction/Refinement during thought:    Should `purpose` be exactly as stated? "buy a bulk order of yarn directly from the factory" is fine.   Should `has_collateral_or_guarantor` be true? Yes, joint liability agreement and group savings act as backing/collateral.   All set. Output matches.    Generate.    `{  "applicant_name": "Adenta Women's Weaving Cooperative",  "amount_ghs": 30000,  "purpose": "buy a bulk order of yarn directly from the factory",  "monthly_profit_ghs": null,  "has_collateral_or_guarantor": true,  "repayment_months": 16}`   Matches schema exactly. No extra text. Valid JSON. Done.    Output matches response.✅   Proceeds.    [Output Generation] -> *Proceeds*   *(Self-Correction/Verification during generation prep)*:    Let's verify the exact wording for purpose: "buy a bulk order of yarn directly from the factory" is directly from the text. Good.   `monthly_profit_ghs`: null. Good.   `has_collateral_or_guarantor`: true. Good.   `repayment_months`: 16. Good.   `amount_ghs`: 30000. Good.   `applicant_name`: "Adenta Women's Weaving Cooperative". Good.   All constraints met. Output matches.✅   Final JSON string construction.✅   Done.    [Output] -> *Proceeds*</think>{  "applicant_name": "Adenta Women's Weaving Cooperative",  "amount_ghs": 30000,  "purpose": "buy a bulk order of yarn directly from the factory",  "monthly_profit_ghs": null,  "has_collateral_or_guarantor": true,  "repayment_months": 16}WARNING: could not parse JSON. Raw output was:<think>Here's a thinking process:1.  **Analyze User Input:**   - **Task:** Extract structured data from a microfinance loan application letter.   - **Schema:** `applicant_name`, `amount_ghs`, `purpose`, `monthly_profit_ghs`, `has_collateral_or_guarantor`, `repayment_months`.   - **Rules:** Use `null` if not stated. Copy numbers exactly. Return ONLY valid JSON.   - **Input Letter:**     "Hi,     This is Kofi. I saw your advert. I want GHS 50,000 to start a car washing business, a     provision shop, and also import phones from Dubai. I am 22 and full of energy. I have not     started any of these yet but my friends say I am very business minded. I will pay back in     one year when the businesses are booming. No collateral but I am trustworthy."2.  **Extract Fields based on Schema:**   - `applicant_name`: "Kofi" (from "This is Kofi.")   - `amount_ghs`: 50000 (from "GHS 50,000")   - `purpose`: "start a car washing business, a provision shop, and also import phones from Dubai" (from "to start a car washing business, a provision shop, and also import phones from Dubai.")   - `monthly_profit_ghs`: null (not mentioned)   - `has_collateral_or_guarantor`: false (from "No collateral but I am trustworthy.")   - `repayment_months`: 12 (from "one year" -> 12 months. Wait, the rule says "Copy numbers exactly as stated." If it says "one year", should I convert to 12 or keep it as null/string? The schema says `number or null`. I'll convert "one year" to 12, or maybe just put 12. Alternatively, I could put null if it's not explicitly in months, but "one year" is clearly 12 months. I'll use 12. Let's stick to 12.)     Wait, let's re-read carefully: "I will pay back in one year". I'll convert to 12. Or maybe just 12. I'll use 12.     Actually, to be safe with "Copy numbers exactly as stated", if it says "one year", maybe I should just put 12. Or null? The example had "10 months" -> 10. I'll use 12.   Let's double check the exact wording for purpose: "start a car washing business, a provision shop, and also import phones from Dubai"   `has_collateral_or_guarantor`: false   `monthly_profit_ghs`: null   `repayment_months`: 12 (or null? I'll go with 12 as it's a direct conversion of one year. If strict, maybe null, but 12 is standard. I'll use 12.)   Wait, let's look at the rule: "Copy numbers exactly as stated." This usually applies to the amount. For repayment_months, if it says "one year", I'll put 12. Or maybe I should just put 12. I'll stick with 12.   Let's construct the JSON:   ```json   {     "applicant_name": "Kofi",     "amount_ghs": 50000,     "purpose": "start a car washing business, a provision shop, and also import phones from Dubai",     "monthly_profit_ghs": null,     "has_collateral_or_guarantor": false,     "repayment_months": 12   }   ```   Check against rules:   - `applicant_name`: "Kofi" -> correct   - `amount_ghs`: 50000 -> correct (removed comma as it's a number)   - `purpose`: "start a car washing business, a provision shop, and also import phones from Dubai" -> correct   - `monthly_profit_ghs`: null -> correct   - `has_collateral_or_guarantor`: false -> correct ("No collateral")   - `repayment_months`: 12 -> correct (one year = 12 months)   One minor point: "Copy numbers exactly as stated." might mean if it says "50,000", I should output 50000 (JSON numbers don't have commas). That's fine.   For repayment_months, "one year" -> 12 is reasonable. If I want to be strictly compliant with "Copy numbers exactly as stated", maybe I should output null if it's not explicitly in months? But "one year" is a duration. I'll output 12. It's standard.   Let's verify the exact text for purpose: "start a car washing business, a provision shop, and also import phones from Dubai"   All good.   Final JSON construction:   ```json   {     "applicant_name": "Kofi",     "amount_ghs": 50000,     "purpose": "start a car washing business, a provision shop, and also import phones from Dubai",     "monthly_profit_ghs": null,     "has_collateral_or_guarantor": false,     "repayment_months": 12   }   ```   Wait, should `repayment_months` be `null` because it says "one year" and not explicitly months? The prompt says "Copy numbers exactly as stated." If it says "one year", maybe I should just put 12. I'll stick with 12. It's fine.   Let's output only the JSON.   Check format: exactly matches schema keys.   All good.   Proceed.    One thing: "Copy numbers exactly as stated." might imply if it says "50,000", I should keep it as 50000 (JSON number). That's standard.   I will generate the JSON now.    Output matches request.✅   Self-Correction/Refinement during thought:    What if `repayment_months` should be `null` because it's not stated in months? The prompt says "If a field is not stated in the letter, use null." "one year" is stated, so 12 is fine. I'll use 12.   All set.    Output matches exactly.    Proceeds.    [Output Generation] -> JSON only.    `{ "applicant_name": "Kofi", "amount_ghs": 50000, "purpose": "start a car washing business, a provision shop, and also import phones from Dubai", "monthly_profit_ghs": null, "has_collateral_or_guarantor": false, "repayment_months": 12 }`   Looks solid.✅   Note: I'll make sure formatting is strict JSON. No markdown if possible, but the prompt says "Reply with ONLY valid JSON and nothing else." I will output raw JSON.   Done.    Output Generation.    *(Self-Correction/Verification during generation prep)*:    Let's double check the exact wording for purpose: "start a car washing business, a provision shop, and also import phones from Dubai" -> matches.   `has_collateral_or_guarantor`: false -> matches "No collateral".   `monthly_profit_ghs`: null -> matches.   `repayment_months`: 12 -> matches "one year".   All good.    Output matches.    Proceeds.    [Output] -> JSON string.    `{  "applicant_name": "Kofi",  "amount_ghs": 50000,  "purpose": "start a car washing business, a provision shop, and also import phones from Dubai",  "monthly_profit_ghs": null,  "has_collateral_or_guarantor": false,  "repayment_months": 12}`   Ready.    *Self-Correction/Note during thought*: The prompt says "Copy numbers exactly as stated." For `amount_ghs`, it says "GHS 50,000". In JSON, numbers can't have commas, so 50000 is correct. For `repayment_months`, "one year" -> 12 is correct. I'll stick with it.   All good.✅   Output matches request.✅   Proceeds.    [Final Check of the Prompt]: "Reply with ONLY valid JSON and nothing else." -> I will output just the JSON object.   Done.    [Output Generation] -> *Proceeds*</think>{  "applicant_name": "Kofi",  "amount_ghs": 50000,  "purpose": "start a car washing business, a provision shop, and also import phones from Dubai",  "monthly_profit_ghs": null,  "has_collateral_or_guarantor": false,  "repayment_months": 12}ERROR: No valid JSON was extracted for any letter. Cannot display table.| Part 3.2 | This made me to  clean the <think> and </think> so that output wouold be clean |


## **3. Attachment of AI Output Evidence** 

Where required, attach evidence of AI use. Tick all that apply. Add more rows where necessary. 

|**Evidence Type**|**Attached?**<br>**(Yes, No,**<br>**N/A)**|**File Name**|
|---|---|---|
|Link|Yes |Evidence.pdf|



## **4. Student Declaration** 

I declare that: 

|**Response Declaration Statement**| **(Yes/No)** |
|---|---|
|The submitted work is my own work. |Yes |
|Any use of AI tools has been clearly declared in this form. | Yes|
|The prompts, instructions, and AI-generated outputs have been disclosed where applicable. | Yes|
|I reviewed, tested, edited, and improved any AI-generated content before submission. |  Yes|
|My AI usage does not exceed 25% of the entire work.| Yes |
|I understand that undeclared or excessive AI use may be treated as academic misconduct. | Yes |

**Student Signature:** _frank_

**Date:**: 16/08/2026
